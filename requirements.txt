streamlit==1.18.1
tokenizers==0.10.3
cohere==3.0.1  # or the latest compatible version
pinecone-client==2.2.0  # or the latest compatible version
PyPDF2==2.12.0  # or the latest compatible version
langchain==0.0.184
transformers==4.31.0  # or the latest compatible version
numpy<2,>=1  # Ensure compatibility with langchain
